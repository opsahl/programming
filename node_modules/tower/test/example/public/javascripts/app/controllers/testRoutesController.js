var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

App.TestRoutesController = (function(_super) {
  var method, methods, proto, _fn, _i, _len;

  __extends(TestRoutesController, _super);

  TestRoutesController.name = 'TestRoutesController';

  function TestRoutesController() {
    return TestRoutesController.__super__.constructor.apply(this, arguments);
  }

  methods = ["get", "post", "put", "patch", "delete", "head", "getPost", "putPost"];

  _fn = function(method, proto) {
    return proto["" + method + "Method"] = function() {
      return this.render({
        text: "" + method + "Method called"
      });
    };
  };
  for (_i = 0, _len = methods.length; _i < _len; _i++) {
    method = methods[_i];
    proto = TestRoutesController.prototype;
    _fn(method, proto);
  }

  return TestRoutesController;

})(Tower.Controller);
