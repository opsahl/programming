
describe("Tower.Application", function() {
  beforeEach(function() {
    return this.application = Tower.Application.instance().initialize();
  });
  it("should load models", function() {
    return assert.ok(App.Post);
  });
  it("should load controllers", function() {
    assert.ok(App.ApplicationController);
    return assert.ok(App.PostsController);
  });
  return it("should respond to requests", function() {});
});
