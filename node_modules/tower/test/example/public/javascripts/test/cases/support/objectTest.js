
describe('Tower.Support.Object', function() {
  test('isHash (is most likely instantiated as {}, direct Object instance)');
  return test('isObject (Object instance, but may be instance of Object subclass)');
});
