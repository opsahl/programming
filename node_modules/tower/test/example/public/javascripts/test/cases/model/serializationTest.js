var criteria, scope, user;

scope = null;

criteria = null;

user = null;

describe('Tower.Model.Serialization', function() {
  beforeEach(function() {
    return App.User.store(new Tower.Store.Memory({
      firstName: "users",
      type: "User"
    }));
  });
  return test("instance.toJSON", function() {
    return assert.deepEqual((new App.User({
      firstName: "Lance"
    })).toJSON(), {
      id: void 0,
      createdAt: void 0,
      likes: 0,
      tags: [],
      postIds: [],
      updatedAt: void 0,
      firstName: 'Lance',
      rating: 2.5,
      admin: false,
      cachedMembershipIds: []
    });
  });
});
